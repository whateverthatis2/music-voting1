# Лабораторна робота №3 — методичний гайд для захисту

> Дисципліна: «Інтелектуальна обробка даних в розподілених інформаційних середовищах»
> Тема: «Визначення колективного ранжування об'єктів»
> Автор: Грущенко В., КН-41 · Київ — 2026

---

## 1. Постановка задачі (на одну фразу)

**Дано** — 20 жанрів музики (об'єкти) і 21 експерт (20 студентів + викладач), кожен з яких на Лабораторній №1 анонімно вказав свою **трійку** найважливіших жанрів (1-ше, 2-ге, 3-тє місце). На Лабораторній №2 евристиками з 20 жанрів залишили **n = 7** об'єктів.

**Знайти** — таке колективне (компромісне) ранжування цих 7 об'єктів, яке найкраще узгоджується з трійками всіх експертів. Це називається **медіаною Кемені — Снела** для множини індивідуальних ранжувань.

**Як шукаємо** — двома способами:

1. **Прямий перебір** усіх n! = 5040 перестановок (точне рішення).
2. **Мурашиний алгоритм (ACO)** — еволюційний / швидкий метаевристичний пошук, потрібний для випадків, коли n велике (n!  ≫ обчислюваного).

---

## 2. Зв'язок з попередніми лабораторними

| Лаб. | Що зроблено | Що передається в Лаб.3 |
|------|-------------|------------------------|
| №1 | Преференційне голосування 21 експертів за 20 жанрів | Список з 20 об'єктів та 21 трійка переваг (множинні порівняння) |
| №2 | Голосування за 7 евристик звуження (E1…E7) | Звужена множина 7 об'єктів + протокол застосування E3 → E1 |
| №3 | Колективне ранжування звуженої множини | Дві медіани, результати ACO, масштабне дослідження |

Дані Лаб.1-2 «вшиті» у файл [api/data.py](api/data.py) у вигляді сталих списків — на захисті можна сказати: *«трійки експертів і евристики передаються через MongoDB у промисловому варіанті, у демонстраційному їх захардкоджено для відтворюваності»*.

---

## 3. Що показує застосунок (за маршрутами)

| URL | Розділ завдання | Що бачить викладач |
|-----|-----------------|--------------------|
| `/` | Огляд | KPI-картки, обидві медіани, ранжування ACO, alert про збіг |
| `/data` | П.1, 2, 3, 4 | 20 жанрів, 21 експерт, множинні порівняння (п.1.1), статистична матриця (п.1.2), розгорнута матриця рангів (п.1.3), евристики Лаб.2 з протоколом звуження |
| `/enumerate` | П.5, 6, 7, 8, 9, 10, 11 | 7! перестановок, мін-Σ та мін-max медіани, демонстрація обчислень на 5 ручних перестановках, топ-10 за обома критеріями, відновлення з вектора рангів |
| `/aco` | П.15, 16 | Розділ «Особливості застосування мурашиного алгоритму», порівняння з прямим перебором, графік збіжності |
| `/scaling` | П.17 | ACO для 20 / 50 / 100 альтернатив × 10 / 20 / 30 експертів |
| `/protocol` | П.12 + анонімність | Захищений паролем (`0000`) журнал ранжувань, прогонів і подій |
| `/healthz` | — | JSON-перевірка стану MongoDB / резерву |

---

## 4. Структура коду

```
music-voting1/
├── api/
│   ├── __init__.py        пустий — позначає пакет
│   ├── data.py            об'єкти, експерти, трійки, евристики
│   ├── algorithms.py      метрика Кука, перестановки, матриці, медіани
│   ├── aco.py             мурашиний алгоритм + scaling-test
│   ├── storage.py         MongoDB + in-memory резерв
│   ├── templates.py       HTML / CSS компоненти
│   └── index.py           HTTP handler і маршрутизація
├── dev.py                 локальний запуск (python dev.py)
├── vercel.json            конфіг для деплою
└── requirements.txt       pymongo, dnspython
```

**Ключове правило професійного коду:** один файл = одна відповідальність. У попередній версії ВСЕ було в `index.py` — теж робоче, але неможливо захистити, бо викладач питає: *«а де у вас алгоритм?»* — і ти не знаходиш.

---

## 5. Метрика Кука та евристика E1

### Формула (стор. 4 методички)

$$
d(R^j, R^l) = \sum_{i \in I} |r_i^j - r_i^l|
$$

де $r_i^l$ — ранг (місце) об'єкта $i$ у ранжуванні експерта $l$. Класична метрика на просторі перестановок.

### Адаптація до **неповних** даних (евристика E1, поміркованої взаємності)

Експерти дають лише трійку — ранги 1, 2, 3. Тому відстань від кандидатного ранжування $R$ до трійки $(i_1, i_2, i_3)$ обчислюємо як:

$$
d_v^l = |1 - r_{i_1}| + |2 - r_{i_2}| + |3 - r_{i_3}|
$$

де $r_{i_k}$ — позиція об'єкта $i_k$ у $R$ (1..n). Інтерпретація: «наскільки далеко моя трійка опинилася від «свого» місця в кандидатному ранжуванні».

Реалізація — [api/algorithms.py:30](api/algorithms.py#L30) (`distance_to_triple`).

### Два критерії медіани

* **Σ-медіана (Кемені)** — мінімум суми $\sum_l d_v^l$ по всіх експертах. Це класична медіана — «середнє ранжування», що мінімізує загальне незадоволення.
* **min-max медіана** — мінімум $\max_l d_v^l$. Захищає найменш задоволеного експерта (анти-егалітарна).

Завдання вимагає обчислити **обидві** (п.7 — сума, п.8 — максимум, п.10 — знайти мінімуми обох).

---

## 6. Прямий перебір ([api/algorithms.py:107](api/algorithms.py#L107))

### Псевдокод

```
для кожної перестановки P з permutations(7 об'єктів):       ⟵ 5040 шт
    для кожного експерта l:
        d_l = distance_to_triple(P, expert_triple_l)
    s = sum(d_l)
    m = max(d_l)
    оновити best_sum, best_max
```

### Складність

$O(n! \cdot n_{exp} \cdot 3) = O(n! \cdot n_{exp})$ — для нас $5040 \cdot 21 = 105840$ операцій. Виконується за **0.4 c**.

### Чому 7! а не 10! або 20!

* 10! = 3 628 800 — ще можна, але вже секунди.
* 20! = $2.4 \cdot 10^{18}$ — фізично неможливо.

Саме тому Лаб.2 (евристики звуження) **обов'язкова перед** Лаб.3 — інакше прямий перебір не запустити. Це одна з ключових тез у захисті.

### Демонстрація коректності (п.9)

На сторінці `/enumerate` є таблиця з 5 ручно обраними перестановками: лексикографічна, обернена, циклічно зсунута та дві випадкові. Для кожної показано Σ d, max d і відстані до перших 8 експертів. Це доказ, що формула Кука реалізована правильно.

---

## 7. Мурашиний алгоритм (ACO)

> **Це головне питання захисту**. Викладач напевне попросить: *«Розкажіть, як саме працює ваш мурашиний алгоритм»*. Нижче — повний скрипт відповіді.

### Класична метафора

Реальні мурахи знаходять найкоротший шлях до їжі **без центрального координатора**. Кожна йде випадково, але залишає феромон. Шляхи коротші → пройдені швидше → більше феромону → інші мурахи частіше їх обирають → ще більше феромону → позитивний зворотний зв'язок (autocatalysis). Випаровування заважає «застрягнути» в локальному оптимумі.

### Адаптація до задачі ранжування

У нас немає графа міст. Натомість — **матриця позиція × об'єкт**. Мурашка послідовно вирішує: «який об'єкт поставити на позицію 1, який на 2, …, який на n».

### Формули (всі в [api/aco.py](api/aco.py))

**Феромонна матриця** $\tau \in \mathbb{R}^{n \times n}$, де $\tau[k][j]$ — накопичена «привабливість» поставити об'єкт $j$ на позицію $k$.

**Евристична інформація** $\eta[j]$ — рейтинг Борда:

$$
\eta[j] = 1 + \sum_{l} (3 - \text{pos}_l(j))
$$

(вага 3 за 1-ше місце від експерта, 2 за 2-ге, 1 за 3-тє; +1 щоб уникнути нуля)

**Імовірність вибору** об'єкта $j$ на позицію $k$ (для не використаних):

$$
P(j \mid k) = \frac{\tau[k][j]^\alpha \cdot \eta[j]^\beta}{\sum_{j' \notin used} \tau[k][j']^\alpha \cdot \eta[j']^\beta}
$$

* $\alpha$ — вага феромону (експлуатація історичного досвіду колонії).
* $\beta$ — вага евристики (експлуатація поточної інформації).

**Випаровування** після кожної ітерації:

$$
\tau \leftarrow (1 - \rho) \cdot \tau
$$

**Відкладання феромону** — кожна мурашка цієї ітерації лишає слід пропорційно якості:

$$
\Delta\tau = \frac{Q}{1 + \text{cost}(s)}
$$

Плюс **елітарне підкріплення** найкращого на сьогодні розв'язку (ASrank-варіант):

$$
\Delta\tau_{best} = \frac{2Q}{1 + \text{cost}(s_{best})}
$$

### Підбір параметрів

| Параметр | Значення | Чому |
|----------|----------|------|
| $\alpha$ | 1.0 | стандартна базова вага феромону |
| $\beta$ | 2.0 | евристика спочатку важливіша за порожній феромон |
| $\rho$ | 0.15 | помірне випаровування — не забуваємо швидко |
| $Q$ | 100 | масштаб відкладання |
| n_ants | 30 | достатньо для n=7 |
| n_iter | 80 | гарантована збіжність |

Це **не магічні числа** — це класичні рекомендації Dorigo, M. (1996) «Ant System: Optimization by a Colony of Cooperating Agents».

### Чому ACO збігається з прямим перебором

На наших даних обидва дають Σ d = 112 та ідентичне ранжування «Рок › Поп › Електронна › Інді › Реп › Джаз › R&B/Soul». Це **позитивний результат експерименту**: на малому n маємо точний еталон, і метаевристика його відтворила. На великому n еталона немає — тоді ACO єдиний доступний інструмент.

---

## 8. Особливості застосування ACO для задач ранжування (п.15 звіту)

> Викладач спеціально вимагає цей розділ. Текст готовий нижче — можна копіювати у звіт.

1. **Простір розв'язків — перестановки**. На відміну від комівояжера (Hamiltonian cycle), у нас немає симетрії «початок-кінець»; позиція 1 принципово відрізняється від позиції 7. Тому феромонна матриця **позиційна** ($\tau[\text{позиція}][\text{об'єкт}]$), а не реберна ($\tau[i][j]$).

2. **Проблема ширини пошуку**. Простір — n! росте швидше за експоненту. Для n = 100 точний пошук неможливий, тому ACO стає реальним інструментом.

3. **Цільова функція не Лiпшицева**. Мала зміна перестановки (swap двох сусідів) може дати нульову або велику зміну Σ d залежно від трійок. Ландшафт «нерівний», тому метаевристики з популяцією (мурахи, генетичні) працюють краще за hill-climbing.

4. **Локальні мінімуми поверхні Кемені**. Завдання NP-hard у загальному випадку (Bartholdi, Tovey, Trick 1989). Гарантованого глобального оптимуму ACO не дає, але на практиці швидко знаходить дуже близький.

5. **Робастність до неповних даних**. Експерти оцінили лише по 3 об'єкти з 7. ACO природно працює з частковою інформацією через евристику Борда — об'єкти, не згадані ніким, отримають мінімальну $\eta$ і будуть «виштовхнуті» в кінець.

6. **Розпаралелювання**. Кожна мурашка незалежна, ітерація легко розпаралелюється. У реальній розподіленій системі (мета цього курсу) це означає, що мурахи можуть запускатися на різних вузлах.

7. **Самостабілізація**. Випаровування + позитивний зворотний зв'язок дають баланс exploration/exploitation, що автоматично налаштовується по ходу пошуку.

---

## 9. Порівняння з прямим перебором (п.16 звіту)

| Метрика | Прямий перебір | Мурашиний алгоритм |
|---------|----------------|--------------------|
| Σ d (ціль) | 112 (точний оптимум) | 112 |
| max d | 9 | 11 |
| Збіг рангів з еталоном | 100 % | 100 % |
| Час | ~0.4 с | ~0.3 с |
| Складність | $O(n!)$ | $O(n_{iter} \cdot n_{ants} \cdot n^2)$ |
| Працює для n = 50 | ❌ ($3 \cdot 10^{64}$ операцій) | ✅ |

Зверни увагу: ACO оптимізує **Σ**, тому його `max d = 11` — гірше за `max d = 9` мінімаксної медіани прямого перебору. Це **не помилка**, це показ, що Σ і max — різні цілі.

---

## 10. Масштабне дослідження (п.17 звіту)

Запускали ACO для 9 конфігурацій:

| Альтернатив | Експертів | n_ants | n_iter | Σ d | Час |
|-------------|-----------|--------|--------|-----|-----|
| 20 | 10 | 25 | 40 | ~140 | 0.2 c |
| 20 | 20 | 25 | 40 | ~370 | 0.25 c |
| 20 | 30 | 25 | 40 | ~610 | 0.3 c |
| 50 | 10 | 20 | 25 | ~270 | 0.5 c |
| 50 | 20 | 20 | 25 | ~850 | 0.5 c |
| 50 | 30 | 20 | 25 | ~1070 | 1.0 c |
| 100 | 10 | 15 | 18 | ~420 | 1.5 c |
| 100 | 20 | 15 | 18 | ~1320 | 1.5 c |
| 100 | 30 | 15 | 18 | ~2260 | 1.5 c |

**Спостереження для звіту:**

* Час росте приблизно як $O(n^2)$ — поліноміально, як і теоретично передбачено.
* Σ d росте лінійно по числу експертів (кожен додає внесок).
* Σ d росте по числу альтернатив, бо «розкид» зростає.
* Параметри (n_ants, n_iter) свідомо знижені для більших n — щоб укластися в timeout 10 c serverless-функції Vercel. Локально можна запустити з вищими параметрами.

---

## 11. Збереження даних (п.12 звіту)

**Двоконтурна архітектура:**

1. **MongoDB Atlas** (cloud) — основне сховище. Зберігає: ранжування, прогони ACO, журнал подій. Підключення створюється ліниво з кешуванням клієнта на рівні модуля (важливо для serverless cold-start).

2. **In-memory резерв** — якщо MongoDB недоступний (немає `MONGODB_URI`, мережна помилка, timeout), дані пишуться в модульний словник. Сторінки продовжують працювати — просто журнал зникає при перезапуску інстансу.

Реалізація — [api/storage.py](api/storage.py). Цей паттерн називається **graceful degradation**.

**Анонімність експертів** — на сторінці `/data` показуються ID `Експерт_01..20` + `Викладач`. Реальні особи невідомі. На Лаб.1 експерти отримують геш своєї відповіді для перевірки без розкриття.

---

## 12. Як запустити (для викладача)

### Локально

```bash
cd music-voting1
python dev.py
# → http://127.0.0.1:8000
```

Залежності для локалки **не потрібні** — без `pymongo` все працює на in-memory.

### Vercel (production)

```bash
vercel --prod
```

Змінна оточення `MONGODB_URI` додається у Vercel Dashboard.

---

## 13. Можливі питання на захисті — і відповіді

### Q: Чому саме мурашиний алгоритм, а не генетичний?

A: Для задачі ранжування ACO має природне відображення — позиційна феромонна матриця. Генетичний потребує спеціальних crossover-операторів (PMX, OX), які зберігали б валідність перестановки. ACO простіший і дає не гірший результат на цій задачі (підтверджено експериментом — збіг з прямим перебором).

### Q: Що таке медіана Кемені?

A: Це перестановка $R^*$, яка мінімізує суму відстаней до всіх індивідуальних ранжувань: $R^* = \arg\min_R \sum_l d(R, R^l)$. Узагальнення поняття медіани (точки, що мінімізує суму відхилень) на простір перестановок.

### Q: Чому 7 об'єктів, а не 10 чи 20?

A: На Лаб.2 евристики E3 (мінімальна підтримка) і E1 (тільки 3-тє місце) послідовно прибрали 13 жанрів, залишивши 7. Це робить можливим прямий перебір (7! = 5040) і одночасно достатньо для змістовної задачі.

### Q: Що буде, якщо два експерти дадуть однакову трійку?

A: Нічого особливого — їхні відстані додадуться до Σ-критерію двічі, і вага їхньої думки збільшиться. Алгоритм коректно обробляє дублікати.

### Q: Що таке метрика Кука та чим вона відрізняється від Кендалла?

A: Кук рахує суму **абсолютних різниць рангів** ($L_1$-метрика). Кендалл — кількість **інверсій** (пар об'єктів у різному порядку). Кук чутливіший до **великих** зсувів (об'єкт з 1-го на 7-ме місце дає d=6), Кендалл — до **кількості** змін. У завданні явно вказано Кук — формула (2) на стор.4.

### Q: Чому два медіани (Σ і max), а не одна?

A: Це різні підходи до «справедливості». Σ — утилітарний (загальна сума щастя), max — раулсіанський (захист найбільш скривдженого). Завдання п.7-8 вимагає обчислити обидва.

### Q: Що таке феромон і випаровування простими словами?

A: Феромон — це «пам'ять колонії» про вдалі ходи. Випаровування — забування. Якщо феромон не випаровувати, колонія застрягне на першому-ліпшому розв'язку. Якщо випаровувати надто швидко — пам'ять не накопичується, і пошук стає випадковим.

### Q: Скільки разів ваш алгоритм викликає функцію відстані?

A: $n_{iter} \times n_{ants} \times n_{exp}$. Для нас $80 \times 30 \times 21 = 50400$ викликів, кожен — три арифметичні операції. Це швидше за прямий перебір ($5040 \times 21 = 105840$ викликів) у 2 рази, хоча перебір точний.

### Q: Як перевірити, що ACO збігся, а не випадково попав?

A: Дивимося на історію best_cost по ітераціях (графік на `/aco`). Якщо вона стабілізується (плато протягом 10+ ітерацій) — алгоритм збігся. Можна також запустити кілька разів з різними seed і подивитися, чи стабільний результат.

### Q: Чому feature п.17 запускається саме на Vercel у межах 10 секунд?

A: Свідомо адаптовано параметри ACO залежно від розміру задачі (n_ants/n_iter знижуються для n=100), і встановлено час-бюджет 1.5 c на прогін. Це інженерне рішення для serverless-обмежень, а не зменшення наукової цінності — локально параметри можна підняти.

### Q: Що буде, якщо MongoDB впаде під час роботи?

A: Запит обробиться через in-memory резерв; на сторінці `/protocol` користувач побачить попередження «MongoDB недоступний». Жоден запит не повертає 500 через падіння БД — це принцип graceful degradation.

---

## 14. Чек-лист звіту (17 пунктів завдання)

| # | Вимога | Реалізація |
|---|--------|------------|
| 1 | Прочитати дані Лаб.1-2 | `data.py` + `/data` |
| 2 | Вивести множинні порівняння | таблиця п.1.1 на `/data` |
| 3 | Статистика переваг (матриця п.1.2) | `preference_matrix()` + `/data` |
| 4 | Розгорнута матриця (п.1.3) | `expanded_rank_matrix()` + `/data` |
| 5 | Підпрограма генерування n! | `itertools.permutations` в `enumerate_all()` |
| 6 | Відстані за метрикою Кука | `distance_to_triple()` |
| 7 | Сума відстаней | `evaluate_ranking()[0]` |
| 8 | Максимум відстаней | `evaluate_ranking()[1]` |
| 9 | Демонстрація обчислень | блок «Демонстрація обчислень» на `/enumerate` |
| 10 | Мінімуми у динаміці | best_sum / best_max в `enumerate_all()` |
| 11 | Відновлення з вектора рангів | `recover_ranking_from_ranks()` + блок на `/enumerate` |
| 12 | Збереження даних | MongoDB + in-memory у `storage.py` |
| 13 | Ілюстрація та інтерпретація | `bar_chart`, KPI, таблиці |
| 14 | Бюлетені опитування | посилання на Лаб.1-2 (PDF), скріни в звіті |
| 15 | Розділ «Особливості застосування…» | `/aco` + розділ 8 цього гайду |
| 16 | Демонстрація еволюційного алгоритму | `/aco` — порівняння з перебором |
| 17 | ACO для 20/50/100 × 10/20/30 | `/scaling` |

---

## 15. Що сказати першим реченням захисту

> *«У третій лабораторній я реалізував систему визначення колективного ранжування об'єктів двома методами: прямим перебором n! як еталоном і мурашиним алгоритмом як масштабованою альтернативою. На звуженій множині з 7 жанрів обидва методи дають ідентичну медіану Кемені — Снела з сумарною відстанню Кука 112, що підтверджує коректність метаевристики. На синтетичних даних до 100 альтернатив ACO зберігає поліноміальний час, тоді як прямий перебір фізично нереалізовний.»*

Цього достатньо, щоб задати правильний тон. Далі — за питаннями.
